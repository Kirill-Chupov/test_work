# Тестовое задание

## [Задача 1](https://github.com/Kirill-Chupov/test_work/tree/main/1)
**Класс Foo:**
1) Необходимо объявить дестуктор виртуальным
2) Оператор **delete** заменить на **delete[]**

**Класс Bar:**
1) Необходимо наследовать публично, для полиморфоного обращения к Bar через указатель на Foo
2) В конструкторе Bar, требуется вызвать конструктор Foo, до инициализации потомка.
3) Оператор **delete** заменить на **delete[]**

**main:**
1) UB(неопределенное поведение) дважды освобождается память по указателю b
2) При присвоении указателю f значения b, необходимо убрать разыменование иначе будет slicing.
3) Потенциальная утечка памяти, new Foo(100) не очиститься
4) void main() использовался в C, необходимо void заменить на int


## [Задача 2](https://github.com/Kirill-Chupov/test_work/tree/main/2)

**Есть два пути:**
1) Вынести интерфейс CodeGenerator в абстрактный класс и реализовать через публичное наследование потомков под каждый язык.
2) Применить паттерн Bridge, через идиому PImpl (указатель на реализацию) и скрыть детали реализации

Но Полиморфизм(1 вариант) в данной задаче предпочтительнее.

## [Задача 3](https://github.com/Kirill-Chupov/test_work/tree/main/3)

**В легаси:**
1) Убрать магическую переменную, заменить на именнованную константу

**В модерн:**
1) Убрать объявления макроса, использовать именнованную константу из легаси
2) Сырые указатели заменить на умные, например **std::make_unique<MyBlah>**
3) Заменить контейнер на вектор, обращение идёт по индексам, ассоциативный контейнер избыточен.
4) Добавить **#include <vector>**
5) Вынести инициализацию в отдельный метод **Adapter::Init()**

## [Задача 4](https://github.com/Kirill-Chupov/test_work/tree/main/4)
1) Необходимо вынести работу геометрическим приметивами в наследование от абстактного класса Shape.
2) Перечесление типа кажется избыточным, за счёт полиморфизма знать конкретный тип не потребуется.
3) Работу с точкой вынести в отдельную структуру Point
4) Реализовать сериализацию в файл для каждго наследник Shape, через Boost.Serialize
5) Заменить поля на приватные, защищеные поля нарушают инкапсуляцию
6) В методе **Feature::draw** вызвать полиморный метод **Shape::Draw()**
7) Изменить метод **Feature::read(File* file)** на новую реализацию с учётом, серализации наследников Shape.

## [Задача 5](https://github.com/Kirill-Chupov/test_work/tree/main/5)
Реализовал упрощённый вариант, только массива int-ов.\
Если значение найдено возвращает позицию элемента, иначе -1.\
Могу реализовать бинарный поиск, но только на плюсах

## Задача 6

Не смогу реализовать работу связного списка, на чистом си.\
На плюсах есть готовый проект.
